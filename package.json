{
  "name": "pigeono",
  "version": "1.0.0",
  "private": true,
  "description": "Pigeono - Multivendor Pigeon Marketplace (MERN)",
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"pnpm --dir server dev\" \"pnpm --dir client dev\"",
    "seed": "pnpm --dir server seed",
    "build": "pnpm --dir client build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
