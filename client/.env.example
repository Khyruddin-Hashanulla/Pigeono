# Backend API origin — set ONLY for split deploys (frontend and backend on
# different domains, e.g. frontend on Vercel + backend on Render).
# Leave unset for same-origin deploys (VPS with Nginx) and local development.
# Example: VITE_API_URL=https://pigeono-api.onrender.com
VITE_API_URL=
